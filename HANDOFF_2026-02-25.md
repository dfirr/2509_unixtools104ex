# Handoff Memo (2026-03-01)

## 1. 今日やったこと
- Ch.3 `03-nmap_hping.qmd` を30分程度の演習教材として成立する水準まで拡張した。
- `nmap` 節をブラッシュアップした。
  - ホスト発見を先に置く構成へ整理
  - `-sn` の実際の挙動、ARP優先、root/non-root の違いを補強
  - Ubuntu 24.04 LTS 前提で `ping` と capability の問題・解答を追加
  - `-sS` と `-sT` のデフォルトと違いを整理
  - `filtered` の説明を修正
  - `-sA` の説明を今回のラボに合わせて是正
  - `-sV`, `-O`, `-Pn`, `-oN`, `-oA` を補強
- `--script` 節を HTTP 系に絞って再構成した。
  - `http-title`, `http-headers`, `http-server-header`
  - `http.useragent`
  - `http-enum` を問題化
  - `robots.txt` の確認例を追加
- `hping3` 節を大幅に拡張した。
  - `nmap` と `hping3` の役割の違いを明記
  - SYN/ACK/FIN の説明と `tcpdump` 観察例を追加
  - `scanner` コンテナ内で実行する手順を明記
- 追加課題を全面更新した。
  - `18000/tcp` の非標準ポート発見
  - `18000/tcp` 側の `/.htpasswd` 露出と `/secret/` の Basic 認証
  - `http-enum` / `http-auth` / `curl` による確認手順
- `まとめ` に後片付けとして `docker compose down` を追加した。

## 2. 演習ラボの現在の構成
- ネットワーク: `10.250.250.0/24`
- 固定IP:
  - `ch3-scanner`: `10.250.250.10`
  - `ch3-target-web`: `10.250.250.20`
  - `ch3-target-closed`: `10.250.250.30`
- `target-web`:
  - `80/tcp`: 既存の HTTP 演習用
    - `/admin.html`
    - `/robots.txt`
  - `18000/tcp`: 追加課題用
    - `/.htpasswd` が露出
    - `/secret/` は Basic 認証
- `80/tcp` 側では `/.htpasswd` と `/secret/` は見えないように分離済み。

## 3. 反映済みコミット
- `main`:
  - `e577489` Start Chapter 3 lab and draft nmap/hping content
  - `cc06ea8` Refine Chapter 3 nmap section
  - `560a366` Expand Chapter 3 scanning exercises
  - `176d60e` Complete Chapter 3 exercises
- `gh-pages`:
  - `d030a39` Built site for gh-pages

## 4. 公開状態
- `main` と `gh-pages` の push は完了済み。
- GitHub Pages は更新済み。
- 対象ページ:
  - `https://dfirr.github.io/2509_unixtools104ex/03-nmap_hping.html`

## 5. Git の注意点
- 途中で `.git/objects` 配下に `root` 所有ディレクトリが混在し、通常の `git add` / `git commit` が壊れていた。
- 原因ディレクトリの所有権は修正済み。
- 一時的に使った `.git/objects/info/alternates` は削除済み。
- 現在は通常の `git status` が通る状態に戻っている。

## 6. 現在の未コミット差分
- `02-tcpdump_nc.qmd`
- `_freeze/02-tcpdump_nc/execute-results/html.json`
- `02-tcpdump_nc_files/`
- `HANDOFF_2026-02-25.md`

注意:
- 上の差分は Ch.3 のコミットには含めていない。
- `02-tcpdump_nc.qmd` と `_freeze/02...` と `02-tcpdump_nc_files/` は、公開処理や render の過程で生じた Ch.2 側の差分。
- 次回開始時に、保持するか破棄するかを判断すること。

## 7. 技術メモ（再実行コマンド）
- Ch.3 ラボ起動:
```bash
cd ~/cases/2509_unixtools104ex/ch3-lab
```

```bash
docker compose up -d --build
```
- scanner へ入る:
```bash
docker exec -it ch3-scanner bash
```
- `admin.html` 発見確認:
```bash
nmap -p 80 --script http-enum target-web
```

```bash
curl -i http://target-web/admin.html
```
- `18000/tcp` の追加課題確認:
```bash
nmap -sV -p 18000 --script http-enum target-web
```

```bash
nmap -sV -p 18000 --script http-auth --script-args http-auth.path=/secret/ target-web
```

```bash
curl -s http://target-web:18000/.htpasswd
```

```bash
curl -i -u student:Password http://target-web:18000/secret/
```
- Quarto ビルド確認:
```bash
docker exec rss sh -lc 'cd /home/rstudio/cases/2509_unixtools104ex && quarto render --to html --no-execute'
```

## 8. 次回の推奨着手順
1. `git status` を見て Ch.2 側の差分の扱いを決める。
2. 必要なら `03-nmap_hping.qmd` の文言を微修正する。
3. 公開更新が必要なら、まず `quarto render --to html --no-execute` を実行する。
4. その後に `gh-pages` 更新を行う。
